# React 19 peer ranges across the @theqrl stack need this, matching the
# myqrlwallet-frontend install.
legacy-peer-deps=true
# Keep Electron's binary download explicit and cacheable.
fund=false
audit=false
